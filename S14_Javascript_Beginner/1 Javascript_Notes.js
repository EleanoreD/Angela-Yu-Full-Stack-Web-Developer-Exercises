// 102
alert('Hello World');
// Hit shift and enter to do multi line code in the console

// Click on sources in the dev tools and then find snippets to run code

// Long form 
window.alert('message');


/** alert is used to ensure some info comes throughtot he user
 * confirm is used to ensure the user confirms comething it returns a boolean
 * promt is used for user input OK returns the value cance returns null
 */

// Short form
alert('message');

// 103 Datatypes: Strings, integers and booleans

// 104 Variables
prompt ('What is your name?');
var myName = 'Angela';

/* 105 Naming Conventions
ctr+k clears console. You cannot use any key words and they also can't begin with an integer */

// 106 Concatenation
alert (message = 'a' + 'b');

// 107 String legnths and retrieving number of characters. 
var tweet = prompt
var tweetCount = tweet.length;
alert('you have written' + tweetCount - (140 - tweetCount) + remaining);

// 108 Slicing a String
bread.slice(x,y);
alert(prompt('Compose your tweet').slice(0,140));

// 108 Concatenation 
alert(message ='a'+'b');

// 109 String Lengths + Retrieving Number off Characters 
var tweet = prompt
var tweetCount = tweet.length;
alert ('You have written' + tweetCount - (140 - tweetCount) + 'remaining';

// 110 Slicing a String
bread.slice(x,y);
Alert (prompt('Compose your tweet.').slice(0,140));

// 111 Case
myName.toUpperCase();
To save as a variable
myName=nameToUpperCase();
var firtChar = myName.slice(0,1);
var upperCaseFirstChar = firstChar.toUpperCase;
var restOfName = myName.slice(1,myName.length);

// git process
// git add ..
// Git commit -m ‘message’
// Git push

// 113 Adding Integers

// 114 Increment / Decrement

var x = 5;
x = x + 1; 
// x will equal 6 after this
// Another cleaner way to do this
var x=5;
x++; 
// increments by one so / 6

var x=5 y=3;
x += y;
// 8 += adds the value after the expression


// 115 Functions - Creatong and Calling
function getMilk(){
//function here//
};

function getMilk(bottles){
var cost = bottles * 1.5;
};

getMilk(2);

121 Functions Return and Outputs
126 Control Statements


if (track == 'clear' {goStraight();
} else {turnRight();
};





